[project]
name = "expense-tracker-mcp"
version = "0.1.0"
description = "A FastMCP server for tracking and summarizing personal expenses."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=3.3.1",
]
